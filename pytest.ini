[pytest]
pythonpath = .
env_files =
    .env
    .env.test
markers =
    integration: integration tests
    manual: manual (opt-in) tests, skipped by default unless explicitly selected or --run-manual

log_cli = true
log_cli_level = INFO
log_level = DEBUG    
